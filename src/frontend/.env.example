# Azure OpenAI Configuration (Optional - for local development only)
# These values will NOT be included in production builds
# Users must configure their own credentials through the Settings page in the app

# Your Azure OpenAI endpoint (e.g., https://your-resource.openai.azure.com/)
VITE_AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# Your Azure OpenAI API key
VITE_AZURE_OPENAI_API_KEY=your-api-key-here

# Your deployment name (e.g., gpt-5.1)
VITE_AZURE_OPENAI_DEPLOYMENT=gpt-5.1

# Note: If Azure OpenAI is not configured, the app will fall back to rule-based coaching
